//삼항 연산자 `?`
let x = 10;
let result = x > 5 ? "크다" : "작다"; //"크다"가 할당됨.-> 참이면 앞에거!

console.log(result);//그래서 크다가 콘솔로그에 찍히겠지? 

let y = 20;
let result2 = y < 10 ? "작다" : "크다"; //"작다"가 할당됨.->거짓이면 뒤에거.

console.log(result2);

